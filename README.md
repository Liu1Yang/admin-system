# admin-system

?? Spring Boot ????????????????????

## ???

- Spring Boot 2.7
- MyBatis-Plus
- MySQL
- Redis
- JWT + BCrypt
- Knife4j?OpenAPI3?
- Maven

## ????

- ???? / ?? / ??????
- ?? CRUD????????
- RBAC ????????????????????403?
- ?? / ???????????????
- ?????? CRUD
- ?? CRUD????????????????????????????????????? Redis ??
- JWT ??????Redis Token ?????????
- Redis ???????????
- ??????????????
- Knife4j ????
- ??????????

## ????

- JDK 8+
- Maven 3.6+
- MySQL 5.7+ / 8.0
- Redis 5.0+

## ????

### 1. ??????

????????? `sql/init-phase-a-order.md`??

```text
sql/init.sql
sql/update-password-bcrypt.sql   ? ???????? 123456
sql/rbac.sql
sql/category.sql
sql/product.sql
```

### 2. ????

?? `src/main/resources/application-dev.yml`?

- MySQL ?????
- Redis ????? `localhost:6379`?

### 3. ????

```bash
mvn spring-boot:run
```

?? IDEA ??? `AdminApplication`?

### 4. ????

| ?? | ?? |
|------|------|
| http://localhost:8080/doc.html | Knife4j ???? |
| http://localhost:8080/api/health | ???? |

## ????

```bash
mvn clean package -DskipTests
java -jar target/admin-system-1.0.0.jar
```

## ????????

| ?? | ?? |
|------|------|
| ?? | POST `/api/auth/login`?`/api/auth/register`?POST `/api/auth/logout`?GET `/api/auth/me` |
| ?? | GET `/api/users`?????CRUD `/api/users/{id}` |
| ?? | GET `/api/roles`?POST `/api/roles`?GET `/api/roles/{id}` |
| ???? | GET `/api/users/{id}/roles`?POST `/api/users/{id}/roles` |
| ?? | GET `/api/categories/tree`?CRUD `/api/categories/{id}` |
| ?? | GET `/api/products`?GET `/api/products/{id}`?POST `/api/products/{id}/cover`?PUT `/api/products/{id}/status`?POST/PUT/DELETE |
| ?? | POST `/api/files/upload` |

??????????????????????? Header ???

```text
Authorization: Bearer {token}
```

## ??????? A?

```text
Client (Postman / ?? Vue)
        ?  Authorization: Bearer {token}
        ?
?????????????????????????????????????????????
?  Interceptor ?                            ?
?  JwtInterceptor ? PermissionInterceptor   ?
?  (+ Redis Token ?????)                  ?
?????????????????????????????????????????????
        ?
 Controller ? Service ? Mapper ? MySQL
        ?              ? Redis???/?????Token ????
        ?
 Result<T> ???? + GlobalExceptionHandler
```

**????** user?role?permission?user_role?role_permission?category?product

## ?? A ???Day20?

1. ?? MySQL + Redis???????? SQL
2. `mvn spring-boot:run` ? IDEA ?? `AdminApplication`
3. Postman ???
   - `postman/PhaseA.postman_collection.json` ? **?????????**
   - `postman/RBAC.postman_collection.json` ? RBAC ???Day13?
4. ??????? PhaseA?**1-?? ? 2-?? ? 3-?? ? 4-?????**
5. ?? Test ?? = ?? A ??????

> ????? HTTP ????? 200????? JSON ?? `code` ???401/403/404 ???

## RBAC ?????Day13?

1. ?????????? `sql/rbac.sql`
2. Postman ? Import ? ?? `postman/RBAC.postman_collection.json`
3. ?????????**0-??** ? **1-??** ? **2-?? 403** ? **3-??? 200** ? **4-????**
4. MySQL ????? `sql/rbac-verify.sql`

> ????? HTTP ????? 200????? JSON ?? `code` ???401/403 ???

## ????????? A ??????????

**?????** ?????????

**????** Spring Boot 2.7?MyBatis-Plus?MySQL?Redis?JWT?BCrypt?Knife4j?Maven

**?????**

- ?? Spring Boot ?? RESTful ????????? Controller-Service-Mapper ????
- ?? RBAC ???????-??-????????? + ???????????401/403?
- ?? JWT ??????Redis Token ??????????BCrypt ??????
- ????????????? CRUD???????????????????
- ?? Redis Cache-Aside ????/????????????????
- ?????????????????? Knife4j ?????????????

**????????????**

- ?????????????????? REST API ??
- ?? 7 ???????????? MyBatis-Plus ?????
- ?? Postman ?? RBAC ????????

## ????

???? LiuYang ? Spring Boot ???????????? API ? JWT?Redis ???????
